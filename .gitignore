# Скачанные таблицы и рабочий кэш — только в корне проекта.
# Косая черта в начале обязательна: без неё правило поймало бы и docs/data,
# а там лежит schedule.json — единственный источник данных для сайта.
/data/

__pycache__/
*.pyc
.venv/

# Подробное описание проекта. Лежит только на диске у того, кто ведёт проект:
# в публичном репозитории его быть не должно — по нему сайт находили в поиске
# вместо самого расписания.
/DEVELOPMENT.md
