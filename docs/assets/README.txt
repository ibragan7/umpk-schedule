logo.png    — логотип в шапке сайта (512 px по длинной стороне)
favicon.png — иконка вкладки браузера (64 px)

Оба сделаны из «Логотип 3. png.png», исходник оставлен здесь же.
Чтобы заменить логотип, положите новый файл и пересоберите копии:

    python tools/make_logo.py "web/assets/новый-логотип.png"

Скрипт сам обрежет прозрачные поля и уменьшит картинку — класть
в шапку исходник на несколько мегабайт не нужно.

icon-student.png — скачанная картинка студента. Не используется:
карточки «Я студент» и «Я преподаватель» сейчас без картинок.
Файл оставлен на случай, если иконки захочется вернуть.
